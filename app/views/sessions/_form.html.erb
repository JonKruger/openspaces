<%= form_for(@session) do |f| %>
  <% if @session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <% @session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :owner %><br>
    <%= f.text_field :owner %>
  </div>
  <div class="field">
    <%= f.label :twitter_handle %><br>
    <%= f.text_field :twitter_handle %>
  </div>
  <div> class="field">
    <%= f.label :timeslot %>
    <%= f.select :timeslot, :options_for_select => TimeSlot.all.map { |ts| [ts.start_time.to_s + " - " + ts.end_time.to_s, ts.id] } %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
